{
  "version": 2,
  "framework": null,
  "installCommand": "pip install uv --break-system-packages && uv venv && uv pip install zensical",
  "buildCommand": "uv run zensical build",
  "outputDirectory": "site"
}
